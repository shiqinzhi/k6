1. go mod的机制
   以 完整的域名+路径 形式导入
这里的域名是 ###github.com###，是个明确的公共代码托管平台，Go 工具链默认支持这些主流的公共代码托管平台（github.com、gitlab.com、bitbucket.org 等），它知道怎么去抓源码，直接用 Git 克隆仓库。
所以这种情况下不用任何额外机制，直接去 GitHub 克隆代码。

import "go.k6.io/k6"
这个域名 go.k6.io 并不是大家熟悉的公共代码托管平台，也不是 GitHub、GitLab 这类平台。

这时候，Go 工具链不知道怎么直接去拿代码。

于是它会去请求：
https://go.k6.io/k6?go-get=1
去获取这个地址返回的 HTML meta 标签，里面会告诉它：
真实的代码托管地址在哪里（比如 GitHub）
用什么版本控制系统（git、hg等）
 
如何验证：   
          这个是302  其实等同于  curl -L https://go.k6.io/k6?go-get=1
        响应是
 curl -L https://go.k6.io/k6\?go-get\=1  重点是:go-import这句
```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8"/>
    <meta name="go-import" content="go.k6.io/k6 git https://github.com/grafana/k6"> 
    <meta name="go-source" content="go.k6.io/k6 https://github.com/grafana/k6 https://github.com/grafana/k6/tree/master{/dir} https://github.com/grafana/k6/blob/master{/dir}/{file}#L{line}">
    <meta http-equiv="refresh" content="0; url=https://github.com/grafana/k6">
  </head>
  <body>
    Redirecting to <a href="https://github.com/grafana/k6">https://github.com/grafana/k6</a>...
  </body>
</html>
```

2   复制一个项目后要改两个地方
1. go.mod中的module要改，
2. 然后再就是整个项目中import的地方要改 
3.  go mod tidy
如何检查呢， go mod graph | grep go.k6.io/k6 


3  间接依赖中又依赖于原来的k6,

因为 Go 的 internal 是根据 import 路径来判断“是否属于模块”的。即使你替换了 module 来源，import 路径没有变，Go 仍然认为是“外部模块访问 internal 包”，
Go 的 internal 检查是基于 import path，而不是 file 实际来源
请看下面的问题
❯go mod graph | grep go.k6.io/k6
输出结果如下:
go.k6.io/k6 go.k6.io/k6@v1.0.0
github.com/grafana/xk6-dashboard@v0.7.11 go.k6.io/k6@v1.0.0
github.com/grafana/xk6-redis@v0.3.4 go.k6.io/k6@v0.51.1-0.20240606120708-bd114fdbd683
github.com/mstoykov/k6-taskqueue-lib@v0.1.3 go.k6.io/k6@v0.53.0

4 既然我已经 go mod vendor 把依赖都复制到 vendor/ 下了，那我在 go build 的时候肯定会默认用这些 vendor 下的内容么？
真相：Go 默认不会使用 vendor/ 目录！
除非你显式指定 -mod=vendor，或者设置了 export GOFLAGS=-mod=vendor
  go build -mod=vendor
否则即使你运行过 go mod vendor，Go 默认仍然使用模块缓存（比如 $GOPATH/pkg/mod）来解析依赖

5 构建过程--***那到底 Go 是怎么找到 main 函数的？***
每一个目录是一个 Go 包（一个 package）
每个目录下的 .go 文件应该有相同的 package xxx 声明
***构建的时候是以目录为单位的，而不是以单个文件为单位****

go build ./examples/foo
构建的是 ./examples/foo 目录下的 Go 包（不管你当前在哪）

Go 并不是在全局搜索 main 函数
它只构建你指定的「包」（也就是一个目录），然后：
如果这个包是 package main
并且有 func main()
那就会构建出一个可执行程序

如果你在没有指定参数的情况下运行：
go build
Go 会：
构建当前目录作为一个包
自动把***当前目录（不包括子目录）***下所有以 .go 结尾的源码文件（不包括测试文件）都加入编译
如果当前目录是 package main，就尝试构建可执行文

编译的时候，其实是允许module的循环依赖（不过这个循环依赖不可以是当前工程的，只能是历史的版本的）
比如当前k6->依赖xk6-dashboard->k6的@v1.0.0版本 

但是你要是 k6@v1.0.0->依赖于xk6-dashboard@v2.0.0->k6的@v1.0.0版本  这就是完全不允许的
质上 —— Go module 构建模型必须满足三件事：
✅ 依赖图必须是可解析（Resolvable）
✅ 版本选择必须是确定的（Deterministic）
✅ 构建必须是可终止的（No infinite recursion）


***-mod模式	含义***
mod	默认行为。根据 go.mod 中的版本进行模块解析，必要时更新 go.sum。
readonly	只读取 go.mod 和 go.sum，不允许写入或修改，否则报错。
vendor	忽略 go.mod 解析，使用 vendor/ 目录中的代码。


go run -mod=readonly main.go
Go 会读取 go.mod 和 go.sum 中声明的依赖版本和校验信息，用来下载和验证依赖模块
因为 CI 中的核心目标是验证提交是否干净、依赖是否完整。
如果用了 -mod=mod，哪怕 go.mod 和 go.sum 有问题，***Go 可能会帮你悄悄补全 —— 这在 CI 中不被允许***。
工程依赖了 A，A 依赖了 B。但 A 升级了版本，B 的版本其实也发生了变化（比如换了实现库），


5  golangci-lint的安装与使用
curl -sSfL https://raw.githubusercontent.com/golangci/golangci-lint/master/install.sh | sh -s -- -b $(go env GOPATH)/bin v2.1.6
golangci-lint --version
运行 linter 检查项目代码
golangci-lint run

golint 自 2020 年后就不再积极维护了，官方已经明确建议使用更现代的工具，比如：
✅ staticcheck
✅ golangci-lint

golangci-lint 是唯一的、核心的产品，其他没有被广泛推广或不是官方统一命名风格下的工具。golangci 这个名字主要是品牌名或组织名，而不是一个工具家族的统一前缀。


6 go.mod 锁住的是版本，go.sum 锁住的是来源和完整性，
   这两个层次你只控制了一个。

文件	控制内容	类比（快递）
go.mod	依赖列表 + 每个依赖的版本号	我下单了什么、买了哪个牌子
go.sum	每个依赖的来源 + 哈希校验值（完整性）	快递包裹封条的校验码、防篡改条码

***require github.com/evil/dependency v1.0.0***
某天 evil/dependency 的作者在 GitHub 把 v1.0.0 的 tag 改了，偷偷加入恶意代码。
你没动 go.mod，版本号完全一样！
但是：
如果你用 -mod=mod，Go 会拉这个“新 v1.0.0”
发现和 go.sum 里原来的校验值不一样，就会报警或更新
所以——
go.mod 只说：我要用哪一版（v1.0.0）；
go.sum 说：我记得这版的内容是啥，校验过！

7
构建工具生成的 .go 文件触发新的模块下载
项目中用到了像 go generate 生成代码（如 gRPC、protobuf、sqlc），这些 .go 文件在某些情况下会引用新依赖。

你跑完 go generate 后，再执行：
go run -mod=mod main.go
Go 会识别这些 .go 文件中的依赖并补全 go.mod / go.sum，但你自己可能都没注意。

影响：
构建流程顺序不一致导致模块变更
CI/CD 复现失败

这又引申出来一个问题
// +build linux 对于这种，需要提前在平台上补齐，不然肯定出现go.sum不一致了，假如我是windows,开发的时候肯定不会下载这个 
import "github.com/linux/only/pkg

使用 go mod tidy + GOOS=linux 提前补全依赖	预先触发依赖拉取	✅ 推荐
vendor 模式	固定依赖 + 忽略 go.sum	✅ 适合大型项目


8  ***代码结构及共享给其它方***
目录	是否约定俗成	说明
cmd/	✅ 是	多个 CLI 可执行程序的入口目录
internal/	✅ 是	项目私有包，Go 官方强制限制外部访问
examples/	✅ 常见	示例目录，常用于文档和教学
pkg/	✅ 常见	可供外部项目复用的库（你项目中没有）

代码共享的方式：
代码放在 pkg/ 目录下，表示这是对外暴露的公共包，供别的模块导入使用。
Go 的导出规则是“首字母大写”，只有函数名、类型名、变量名等首字母大写，才是包外可见（public）的。

.
├── cmd/            # 每个子目录是一个可执行程序的入口
├── internal/       # 仅供本仓库内部使用的代码（编译器限制外部引用）
├── pkg/            # 可供外部引用的公共代码包
├── api/            # API 接口定义（proto、OpenAPI spec 等）
├── configs/        # 配置文件（yaml、json、toml等）
├── web/            # 前端静态文件、模板
├── scripts/        # 脚本（构建、发布、工具）
├── deployments/    # 部署相关（k8s yaml、docker-compose 等）
├── test/           # 测试辅助代码或集成测试
├── go.mod
├── go.sum
└── README.md


9  ***包名和 import 路径无强制关系，可以不一致***
比如当前项目 go.mod中有一个依赖 github.com/Masterminds/semver/v3 v3.3.1
在luncher.go中有一个使用是这样使用的
"github.com/Masterminds/semver/v3" 
但是在代码中调用的时候是 semver,而这个是包名，所以包名不一定和目录名一致，
使用的时候肯定是包名，导入的时候是目录名
结论： ****使用时的名字是包名（由 package xxx 决定），导入时的路径是源码目录名（用于 Go 找到包位置，含版本后缀）。***

10 ***k6的基本原理***
HTTP/HTTPS 协议的负载测试 —— 绝大部分用例都是 Web 接口的压力和功能测试。
k6 内部自带了一个 JavaScript 引擎（早期是 Goja，纯 Go 实现的 ES5/ES6 引擎，后来增加了部分扩展 API）。
所以当你运行 k6 run test.js 时：
k6 用 Go 编写的核心程序读取你的 test.js
用内置的 Goja 引擎解析和执行 JavaScript
k6 额外提供了 http.get()、sleep() 等特定 API，这些 API 是 Go 语言实现的，然后通过 JS 引擎暴露给脚本用
因此 不需要 Node.js，哪怕你本机完全没有装 Node.js，也可以直接运行 k6


11 ***nocopy的使用，标准库中***
type Mutex struct {
_ noCopy
mu isync.Mutex
}
匿名字段 _ 的好处
用匿名字段 _ 命名，表示这个字段不会占用实际内存空间（空结构体大小为0），不会增加结构体大小。
但是又能触发 go vet 的复制检测。


12  ***解释 interface{} 的作用***
interface{} 叫做 空接口（empty interface），它里面没有任何方法。
因为任何类型都默认实现了「零方法集」的接口（也就是空接口），所以 任何类型的值都可以赋给 interface{} 类型的变量。
其实现在就是 type any interace {}

13  unsafe.Pointer
***unsafe.Pointer 是“通用指针”，只表示“某个内存地址”，但它本身不允许做算术运算，而 uintptr 是整数类型，可以对地址做算术运算***
unsafe.Pointer 是“通用指针”，只表示“某个内存地址”，但它本身不允许做算术运算，而 uintptr 是整数类型，可以对地址做算术运算。

为什么要区分 unsafe.Pointer 和 uintptr？
unsafe.Pointer 表示“内存地址”，但带指针语义。Go 语言的垃圾回收（GC）和内存模型依赖于指针的准确性，编译器和运行时会跟踪指针类型的变量，确保内存安全和 GC 正确回收。
****如果允许对 unsafe.Pointer 做算术运算，会破坏指针语义***
比如对指针做加法、减法、偏移后，指针可能指向“非对象”的地址，GC 无法正确识别。
指针类型的变量必须指向合法的内存对象，否则会出错。
uintptr 是一个普通整数，表示地址的数值：
它没有指针的语义，仅仅是一个数字。
你可以对它做算术运算（加减）来计算偏移。

***但是 uintptr 不被 GC 视为指针，不会阻止所指对象被回收***

*** # (uintptr 是 Go 语言里的无符号整数类型)***
如果你把 uintptr 保存很久或者没有及时转回 unsafe.Pointer，指针指向的对象可能被 GC 回收，导致悬空指针。
Go 官方文档里强调，转换到 uintptr 做算术时，务必立刻转换回 unsafe.Pointer，且不要把 uintptr 类型变量保存下来，否则可能出现安全隐患。

```go
func main() {
	a := []int{1, 2, 4}
	fmt.Printf("数据的长度为:%v\n", len(a))
    //TODO 下面这行  
    // 1 unsafe.Pointer(&a[0]) 这是第一个元素的指针转指针,实现了 具体指针->通用指针
    //2 指针不能运算， 通用指针->uintptr,并做运算 
    // 3 运算的结果uintptr 要尽快转成 "通用指针"  uintptr必须要来转成"通用指针" 
	t := unsafe.Pointer(uintptr(unsafe.Pointer(&a[0])) + unsafe.Sizeof(a[0]))
    //4 通用指针转成具体指针，然后对具体指针进行赋值即可
	*(*int)(t) = 34
}

```

12 为什么 unsafe.Sizeof 返回的是 uintptr 而不是普通的 uint？
func Sizeof(x ArbitraryType) uintptr
我感觉这个ai都没说对，只能说sizeOf本来相当于是指针两减的差值，
能容纳指针并且能做运算的肯定是uintptr了。

13 ***make vs new***
在 Go 里，make 和 new 都是用来分配内存的，但它们的用途、返回值类型、适用对象完全不一样。
***new 只会把分配的内存置为类型的零值，不会帮你把它变成“可用状态”的复杂对象。***
我帮你分成一张对比表，你一眼就能记住：

特性	make	new
用途	初始化内建引用类型（slice、map、chan）	分配任意类型的零值内存
返回类型	返回非指针，是初始化后的对象本身	返回一个指向类型零值的指针
初始化	会做初始化，让 slice、map、chan 可用	只分配内存并置零，不做进一步初始化
适用类型	只能用于 slice、map、chan	可以用于任意类型
零值可用性	返回的值可以直接用（长度、容量等已设定好）	返回的值指向零值，可能还需要进一步初始化

***new 的初始化动作很“懒”：***
数值型 → 0
布尔型 → false
字符串 → ""
指针/引用类型（map、slice、chan、指针）→ nil
而 make 直接帮你把引用类型初始化成可用对象，所以：
只用 new → 你还要自己对引用类型字段 make 一下
用 make → 直接能用，不需要再额外初始

14 ***什么是“零值”***
零值是 Go 语言对某个类型在未显式初始化时所采用的默认值。
语言规范定义了“对于每种类型都有一个零值”，并规定变量在声明但未赋初值时会自动初始化为该类型的零值。零值的含义是“合理的默认值”，而不是内存层面的某种具体比特模式（不要依赖内存比特表示）。

***如何产生零值（声明/构造方式）***
1. var x T → x 被初始化为类型 T 的零值。  ***特别注意 var a Stu这也是完全没问题的***
2. 局部短变量声明 x := T{}（复合字面量） → 字面量内未指定的字段为零值。
3. new(T) → 返回 *T，指向一个零值的 T。
4. &T{} → 返回 *T，字段为零值（可以在字面量中初始化字段）。
5. make 仅对 slice/map/chan：make 返回已初始化可用的对象，***而不是“零值”***

***接口的零值（nil）
   意义：接口的零值表示“当前没有绑定任何动态类型和值”。***

在内部实现上，接口值包含两个部分：类型信息 + 值。零值接口的这两个部分都是 nil。
nil 接口意味着“没有对象符合这个接口”，常用来表示没有行为对象或错误不存在

指针变量的类型 vs 值
var p *int = nil
p 的静态类型是 *int，这是编译时确定的，存在符号表和编译信息里
p 这个变量本身的运行时值是一个内存地址（0 表示 nil）
指针变量只存储值（地址），没有同时存类型信息在它变量本身里
换句话说，“类型”信息在编译期确定，运行时变量 p 只保存“地址”这一项数据

接口变量的类型和值
接口变量在运行时内部保存两个指针（或者说两个字段）：

字段	含义
类型指针	指向接口具体持有的类型信息结构体
数据指针	指向具体值或数据
所以接口变量在运行时本身就是一个包含类型指针和值指针的结构体

*****Go 允许调用 nil 指针的方法，只要这个方法在执行时不访问该指针的字段或调用其方法导致 panic。*******
```go
package  main

import "fmt"

type Stu struct {
	Name string
}

func (s *Stu) test1() string {
	fmt.Printf("hello")
	return "hello"
}

type TestI interface {
	test1() string
}

func main() {
	var a TestI = nil
	var b *Stu
	a = b
	a.test1()
}

```

15  其实就是如何理解  5*time.Seconds这个看上去很简单的玩意
什么是常量表达式（constant expression）？
常量表达式指的是编译期能够确定值的表达式

其实根本原因是5是无类型

1. 常量表达式里的运算，是在底层类型值层面执行的
 因此允许Duration * Duration这种乘法（其实是底层整数乘法），结果还是Duration。
 
如果想运算，必须先转换到底层类型：
var a, b Duration
var c = Duration(int64(a) * int64(b)) // 允许，显式转换到底层类型再运算

看个不能允许的，
var x int64 = 5
var c = x * time.Second

命名类型运算的许可条件
1. Go允许相同命名类型之间的运算，当且仅当：
运算符对该类型的底层类型有效（int64支持*）
运算结果类型与操作数类型相同（结果仍是time.Duration
2. 当表达式包含无类型常量时，Go会按以下顺序处理,会做一些自动转化
   5 * time.Second
   ↑   ↑
   │    └── 已明确是time.Duration类型
   └── 无类型整型常量

   
17 给接口赋值值类型和赋值指针类型有重大区别====特别重要，方法集也不一样，值有可能是复制的
值类型 T 不能调用仅有指针接收者的方法。
指针类型 *T 可以调用值接收者和指针接收者的方法。

Go 接口底层结构（非空接口）
Go runtime 源码里大致是这样（简化版）：

type iface struct {
tab  *itab          // 类型信息 + 方法表
data unsafe.Pointer // 指向具体数据
}
tab（type）
指向一个包含“动态类型信息”和“方法表”的结构。
动态类型信息里会区分 main.MyStruct 和 *main.MyStruct —— 它们是两个完全不同的类型。
data（value）
存放具体值：
如果是值类型赋值给接口，runtime 会在堆上复制一份值，然后 data 指针指向这个拷贝。
如果是指针类型赋值给接口，data 就是直接保存这个指针的值。


18 一般常见的go build的参数
调试的时候是-x -v可以方便观察
但是生产上
-s → strip all symbols（去掉所有符号表）
-w → omit DWARF debug info（去掉调试信息）

***-ldflags "-X <pkg>.<var>=<value>" 本质上就是 在编译链接阶段，把变量值直接写进最终的二进制里***
. 关键细节
main.version 这里的 main 是包名，必须写全路径：
如果在 main 包 → main.version
如果在 pkg/info 包 → your/module/path/pkg/info.Version
变量必须是 string 类型（不是 const、不是局部变量）
必须是可导出或未导出都行，但必须是全局变量
必须在编译目标包中声明（不在依赖包内）

常见生产打包命令模板
版本号来自 git describe --tags
构建时间来自 date
主程序包是 main，有变量：
var (
version   string
buildTime string
)
VERSION=$(git describe --tags)
BUILDTIME=$(date +'%Y-%m-%dT%H:%M:%S')
go build \
-o myapp \
-trimpath \
-ldflags "-s -w -X 'main.version=$VERSION' -X 'main.buildTime=$BUILDTIME'"
二进制小（-s -w）
源码路径剔除（-trimpath）
包含版本信息（-X）
产物命名规范（-o）


18 求值顺序(分别普通求值顺序和包求值)
n0, n1 = n0+n1, n0
这种有明确的顺序，就是
***右边的计算不受左边赋值的影响，因为是先算完右边再赋值。***
这是为什么 n0, n1 = n0+n1, n0 能正确交换值的一部分思路（虽然交换更常见的是 a, b = b, a）。
#
y[f()], _ = g(h(), i()+x[j()], <-c), k() 这种的话，先f->h->i-j->g
 上面这种怎么计算呢，其实从左到右，计算
```go
// chapter3/sources/evaluation_order_4.go
package main

import "fmt"

func f() int {
	fmt.Println("calling f")
	return 1
}

func g(a, b, c int) int {
	fmt.Println("calling g")
	return 2
}

func h() int {
	fmt.Println("calling h")
	return 3
}

func i() int {
	fmt.Println("calling i")
	return 1
}

func j() int {
	fmt.Println("calling j")
	return 1
}

func k() bool {
	fmt.Println("calling k")
	return true
}

func main() {
	var y = []int{11, 12, 13}
	var x = []int{21, 22, 23}

	var c chan int = make(chan int)
	go func() {
		c <- 1
	}()

	y[f()], _ = g(h(), i()+x[j()], <-c), k()
}

```

20 ***go build后面的参数到底是什么含义***
go build 后面的参数到底是什么？
go build 既支持 目录路径，也支持 包路径。
这两者的本质是一样的，因为 Go 的包就是对应一个目录，所以两者在多数情况下是等价的

2. 目录路径和包路径的区别
   类型	形式示例	说明
   目录路径	.、./cmds/client	操作系统文件路径，告诉 Go 在哪个目录下构建包
   包路径	github.com/user/project/cmds/client	完整的模块路径 + 包相对路径，告诉 Go 在模块里定位包
   在goland中还有一个额外的要求，就是run directory的时候，如果你指定了run的directory，那working directory也必须变是因为go build后
   要进行运行，这运行的就是在run 配置的directory下生成的。所以才有了这个额外的run directory与workding directory必须一致的。
 

22 自定义标签的事
标签分为两种，一种是内置的，一种是自定义的
1. 内置的
// +build linux 是 Go 语言的内置平台标签
linux 是 Go 预定义的平台标签之一，表示这个文件只会在 Linux 操作系统环境下编译。
你不需要手动通过 -tags=linux 来激活它，Go 编译器会根据当前系统自动识别

2. 自定义的
-tags 用于启用你自己定义的构建标签，比如：
// +build debug1 （假如你有一个go文件开头这样写)
// go:build debug1 和上面一样，不过这是新的写法  Go 1.17
   这种标签需要你通过 -tags=debug 显式告诉编译器启用


23 select&channel的逻辑
Golang 中 select 语句用于同时等待多个 channel 的操作，理解它的逻辑很关键。
简单来说，select 会 随机 地等待多个 channel 中的任意一个准备好（即可以发送或接收），然后执行对应的 case 代码块。

具体来说，select 的工作流程是：
检查所有 case 中的 channel 是否已经准备好：
对于接收操作 (case v := <-ch)，判断 channel 中是否有数据可以接收。
对于发送操作 (case ch <- v)，判断 channel 是否有缓冲空间或接收者等待。
如果有一个或多个 channel 已经准备好，select 会随机选择一个就绪的 case 执行。
这个“随机”是均匀的，避免了偏向某个 channel。
如果没有任何 channel 准备好，且存在 default case，立即执行 default 分支，不阻塞。
如果没有任何 channel 准备好，且没有 default，select 会阻塞，直到某个 channel 准备好


24  数组指针自动解引用访问元素。
实际上 Go 语言在访问指针指向的数组时，允许用 ptr[index] 的形式直接访问，而不需要显式写 (*ptr)[index]。
规则说明（简化版）
   Go 语言规范允许你对指针类型做数组、切片、字符串、映射、通道的操作时，会自动“解引用”。

如果 ptr 是指向数组的指针，ptr[i] 会自动被解释成 (*ptr)[i]。
这条语法糖是为了方便写代码，不用每次都写繁琐的(*ptr)[i]
```go
var arr [3]int = [3]int{10, 20, 30}
var p *[3]int = &arr

fmt.Println(p[0])   // 自动转换为 (*p)[0]，输出 10  这里
(*p)[0] = 100       // 也可以这样写，修改数组第一个元素
fmt.Println(arr[0]) // 输出 100，说明修改成功

```
但是切片的指针是需要先解引用的，不会被自动解引用
. 注意区分数组和切片
数组指针：*[N]T，可以用 ptr[index] 访问。
切片：[]T 本身就是引用类型，不存在指针再解引用的问题。
切片的指针：*[]T，对这个就不能直接用 ptr[index]，必须先解引用：(*ptr)[index]。

需要查一查 ：***切片没有解引用，但是数组指针有解引用，我还是没特别理解为什么会有这个区别，我以为两个要么都有，两个要么都没有***

25 go private的规则
┌──────────────┐
│   go get xxx │
│ (import路径) │
└──────┬───────┘
│
▼
判断模块路径是否匹配 GOPRIVATE？
/        \
是          否
│           │
│           ▼
│     走 GOPROXY（代理服务器）
│           │
▼           │
绕过代理，直接访问源码仓库  │
│           │
│           ▼
│     代理根据 redirect 重写请求路径（Athens）
│           │
│           ▼
│     代理拉取代码（私有仓库或公共仓库）
│           │
▼           ▼
模块数据返回给 Go 客户端
│
▼
根据模块路径是否匹配 GOPRIVATE 决定是否跳过校验数据库
│
▼
完成模块校验和构建

重点画出来：
1. 模块路径不在 GOPRIVATE → 必定走 GOPROXY，去代理服务器（Athens），
2. Athens 会根据 redirect 规则，把 github.com/grafana/k6 替换成私有仓库地址去拉
3. 模块路径在 GOPRIVATE → 直接去源码仓库拉，绕过 GOPROXY
4. 无论哪条路径，Go 都会根据 GOPRIVATE 判断是否跳过校验数据库
 问题来了，如果是redriect 必须加这个
   添加 GONOSUMDB
   告诉 Go 对这个模块跳过 sumdb 校验：
go env -w GONOSUMDB=github.com/grafana/k6
GONOSUMDB 只控制校验数据库校验，不会影响 GOPROXY 或源码拉取路径。
这样就算模块路径没加 GOPRIVATE，校验时也不会去公共校验数据库查哈希，避免失败。

***GOPRIVATE的作用***
指定私有模块的路径（如公司内部仓库），这些模块将：
1. 绕过GOPROXY直接拉取
2. 跳过GOSUMDB校验
3. 支持通配符（如*.corp.com）

***GONOSUMDB***
明确声明哪些私有模块不需要校验和验证，格式与GOPRIVATE一致
通常与GOPRIVATE联用，避免私有代码被校验数据库记录
若未设置，***默认继承GOPRIVATE的值***

真正用的时候，
# 设置代理链（包含私有代理和公共代理）
go env -w GOPROXY="http://your-private-proxy,https://goproxy.cn,direct"

Go 命令会依次尝试这些代理：
1. 先访问 http://your-private-proxy 代理，找不到或失败则
2. 再访问 https://goproxy.cn 代理，找不到或失败则
3. 直接访问源码仓库（比如 GitHub、GitLab 等）拉取模块代码

direct 是一个特殊值，代表“无代理”，即 Go 命令直接用版本控制工具（git、hg 等）拉取源码。

# 仅关闭目标模块的校验（非全局关闭）
go env -w GONOSUMDB="github.com/original/repo"  # 替换为实际模块名:ml-citation{ref="3,7" data="citationList"}

必须参照这个来配置一下：
https://blog.csdn.net/a772304419/article/details/123201983

21 context 通过父子链式结构和取消信号传播机制实现了调用链中上下游的取消通知
1. context 是树状结构
   每次调用 context.WithCancel(parent)、WithTimeout 或 WithDeadline
，都会基于父 context 生成一个新的子 context，它们之间是树状（链状）关系。

举例：
a := context.Background()
b, cancelB := context.WithCancel(a)
c, cancelC := context.WithCancel(b)
这里 a 是根，b 是 a 的子，c 是 b 的子。

2. 取消信号的传播机制
 b 的 Done channel 会监听 a 的 Done channel，同理 c 会监听 b 的 Done。
当调用 cancelB()（取消 b），会关闭 b 的 Done channel，
c 在监听 b 的 Done channel，一旦它关闭，c 也会感知到父 context 取消。
这样保证了取消信号从上到下“传递”。


22 ***GMP***
P 的数量是固定的，由环境变量 GOMAXPROCS 控制，默认等于 CPU 核数，比如 4 核就是 4 个 P。
M（OS 线程）的数量是动态的，runtime 会根据需要创建多个 M，但同时活动的 M 数量不会超过 P 的数量太多，
通常会多于 P（因为 M 可能会阻塞，比如系统调用阻塞），但空闲的 M 是不绑定 P 的。
每个 P 持有自己的本地 goroutine 队列，用来减少调度器全局队列的竞争

P 是 golang runtime 自己维护的资源池，数量由 GOMAXPROCS 决定，代表***同时能运行****的逻辑处理器个数（默认是 CPU 核数）。
每个 M 线程执行的时候，必须先从可用的 P 池中取一个 P，获得运行资格后，M 才能运行 G。
换句话说，M 作为操作系统线程，执行时必须“绑定”一个 P 才能执行 Go 代码。
如果没有空闲的 P，M 就只能空转等待或者执行系统调用（比如 IO 阻塞等）。

具体怎么“绑定”的？

P 是 runtime 管理的一个调度上下文。
它有自己的本地运行队列（goroutine 队列），以及调度状态。
M 从全局调度器获取一个 P，然后开始循环调度 G。
绑定了 P 的 M 可以拿到要执行的 G，从而执行 goroutine 代码。
M 和 P 是一对一运行关系，不同的 M 不能共用同一个 P，P 限制了并发数。
P 就像“许可证”，有多少个 P，最多就能同时跑多少个 goroutine。

P 是绑定在 M 上执行的，M 执行一个 G
P 其实是一个调度上下文，在任意时刻，一个 P 只能被一个 M 绑定使用，也就是说同一时刻只有一个 M 在用这个 P 来跑它的 goroutine 队列。
P 里面有多个等待执行的 G（goroutine），M 按顺序从 P 的本地队列或者全局队列里取一个 G 执行

某个 G 阻塞了，怎么办？
如果当前正在执行的 G（在 M 上跑）阻塞了（例如调用了阻塞的系统调用或同步操作），当前 M 也会阻塞（线程阻塞）。
这时，runtime 会做“抢占式”处理：
runtime 会把当前 P “脱离”被阻塞的 M，让这个 P 变成“空闲状态”，这样其它空闲的 M 可以去绑定这个 P，继续调度 P 里的其它 goroutine。
也就是说，P 不会因为其中一个 G 阻塞就整体“卡住”，P 会被释放出来，让其它 M 继续执行 P 上其他可运行的 G。

. 详细机制 —— “P 离开阻塞的 M”
具体来说：

当前 M 正执行某个 G，G 阻塞了。

runtime 检测到阻塞，会：
创建新的 M（如果没有足够的空闲 M）。
把当前 P 和这个阻塞的 M 解绑（P 变为可用状态）。
让其他 M 从 P 的队列继续拿 G 执行。
所以阻塞的 M 不再绑定 P，可以去执行系统调用或休眠。
其它 M 绑定该 P，继续调度其他 G

23 GMP
***M的限制机制***
默认数量限制：Go程序启动时，M的最大数量默认设置为10000，但这个值通常远超过操作系统实际支持的线程数限制（如Linux默认约8K线程），因此实际约束主要来自操作系统
动态创建与回收：
当M因系统调用阻塞时，Go运行时会创建新的M来接管其他任务，避免线程闲置
空闲的M会被回收或进入休眠状态以减少资源占用
手动调整：可通过runtime/debug.SetMaxThreads函数修改M的最大数量，但需谨慎操作以避免系统资源耗尽

2. G与GOMAXPROCS的关系
   G的数量：G（Goroutine）的数量理论上仅受内存限制（初始栈约2KB），无硬编码上限
   GOMAXPROCS的作用：该参数控制P（Processor）的数量（默认等于CPU核心数），而非直接限制G的数量。P的数量决定了并行执行的Goroutine最大并发度
   设置GOMAXPROCS=4时，最多有4个P同时调度G到M上执行
   通过环境变量$GOMAXPROCS或runtime.GOMAXPROCS()调整

28 ****Go runtime 是怎么检测某个执行中的 goroutine 阻塞了，从而把它所在的 M 和 P 解绑，把 P 释放给其他线程继续调度？***
. Go 判断阻塞的核心点
Goroutine 阻塞一般是因为：
系统调用（I/O 阻塞，比如读写文件、网络等）
同步原语等待（如 channel 阻塞、mutex 等）
runtime 的某些等待（如 GC 等）

2. 关键：Go runtime 主动知晓阻塞，而非被动检测
   Go runtime 自己控制了哪些操作会阻塞：
对于系统调用阻塞，Go 会用非阻塞的网络轮询器（netpoller）或者异步系统调用来避免 M 直接阻塞。
但是，当 Go 调用可能阻塞的系统调用时，会有特定的协作机制告诉 runtime 这个 M 要阻塞了
   ***怎么实现的？****
   Go runtime 内部有很多对系统调用的封装（比如 runtime.entersyscall、runtime.exitsyscall）。
在调用阻塞的系统调用前，runtime.entersyscall 会把当前 M 标记为阻塞状态，同时解绑 P。

系统调用返回后，runtime.exitsyscall 会将 M 状态恢复，尝试重新绑定一个 P。

29 阻塞的时候，并不一定占用这个线程
一个连接一个 goroutine 是可以的
你完全可以每个连接启动一个 goroutine 来负责收发数据，因为这个 goroutine 在没有数据时会被 runtime 挂起，不会占用 M（线程）。
所以这和“一线程一连接”的传统阻塞模型不同，Go 里的 goroutine 是轻量协程，阻塞不等于线程阻塞。

***网络 I/O 阻塞（绝大多数长连接场景）***
Go 的 net 包并不是直接用阻塞的 read、write 系统调用。
它用的是 非阻塞 fd + netpoller（epoll/kqueue/iocp）。
当 goroutine 在 Read、Write 上“阻塞”时，runtime 会：
把这个 goroutine 从 P 的运行队列里移走（挂到 netpoller 等待队列）。
M 不会阻塞，会继续执行 P 队列里的其他 goroutine。
也就是说，***这种阻塞只是 goroutine 层的，M 会被释放出来继续跑别的任务。****

📌 结果：
****10 万个连接，每个连接一个 goroutine，在无数据时几乎不占线程数，线程数可能就几十个。****
2. ****真正的阻塞系统调用（磁盘 I/O、阻塞 read/write）****
   如果 goroutine 直接调用了阻塞的系统调用（比如文件读写、调用第三方 C 库的阻塞函数），runtime 没法用 netpoller。

在这种情况下：
M 会被系统调用阻塞。
runtime 会先调用 runtime.entersyscall 解绑 P，把 P 给别的空闲 M。
阻塞的 M 就去等系统调用返回（这时它没有 P，也不会调度 Go 代码）。
系统调用返回后，调用 runtime.exitsyscall，M 再尝试绑定一个空闲 P 继续执行。

📌 结果：
***M 会真的阻塞，但不会拖死 P；P 会转移到别的 M 上继续跑 goroutine。****

  核心区别总结
   场景	M 会不会阻塞	P 会不会被释放	说明
   网络 I/O（epoll/kqueue）	❌ 不会阻塞	✅ 会释放	通过 netpoller 等事件唤醒 goroutine
   阻塞系统调用	✅ 会阻塞	✅ 会释放	entersyscall / exitsyscall 解绑、绑定 P


24 range的变化
Go 1.22 之后的变化
从 Go 1.22 开始，for 和 for range 的循环变量在每次迭代时都会重新声明（新变量），
这样闭包捕获到的是这一轮迭代的值，而不是同一个变量的引用

```go
var m2 = [...]int{1, 2, 3, 4, 5}

	for i, v := range m2 {
		go func() {
			time.Sleep(time.Second * 3)
			fmt.Println(i, v)
		}()
	}

	time.Sleep(time.Second * 10)
```
1.22后相当于帮你做了一个新的
```go
var m2 = [...]int{1, 2, 3, 4, 5}

for i_, v_ := range m2 {
    // Go 1.22 的编译器等价于帮你做了这一行：
    i, v := i_, v_  // 每次迭代新建 i 和 v（不同地址）

    go func() {
        time.Sleep(time.Second * 3)
        fmt.Println(i, v) // 捕获的就是这一轮的新变量
    }()
}

time.Sleep(time.Second * 10)

```

23  range副本的事 ，range就是一个副本
```go
var m2 = [...]int{1, 2, 3, 4, 5}
	var b [5]int
    //注意下面这里m2和&m2 
	for i, v := range m2 {
		if i == 0 {
			m2[0] = 10
			m2[1] = 15
		}
		b[i] = v
	}
	fmt.Println("结果是", m2)
	fmt.Println("结果是", b)
```
关于切片的事,看一下下面，其实可以理解m2是切片的一个副本，所以循环的时候长度也不会变
```go
func main() {

	var m2 = []int{1, 2, 3, 4, 5}
	var b []int

	for i, v := range m2 {
		if i == 0 {
			m2 = append(m2, 60, 70)
		}
		b = append(b, v)
	}
	fmt.Println("看一下值:", m2)
	fmt.Println("看一下值:", b)

}
```
当range表达式的类型为数组时，range需要复制整个数组；而当range表达式类型为数组指针或切片时，这个消耗将小得多，因为仅仅需要复制一个指针或一个切片的内部表示（一个结构体）即可
对于range后面的其他表达式类型，比如string、map和channel，for range依旧会制作副本

***for range的channel的时候有特殊的***
channel对于channel来说，channel在Go运行时内部表示为一个channel描述符的指针
当channel作为range表达式类型时，for range最终以阻塞读的方式阻塞在channel表达式上，即便是带缓冲的channel亦

具体原因是 Go 的 channel 内部确实维护了一个“关闭”状态标志：
当你用 for range 读取 channel 时，会不断从 channel 中接收数据。
如果 channel 没关闭但暂时没有数据，接收操作会阻塞等待新数据。
一旦 channel 被关闭，并且里面的数据都被读完了，for range 会自动退出循环。
简单来说，for range 会循环读取直到“channel 关闭且数据读完”为止。
 


25 跳转标签的问题

***break和goto其实不太一样的***

你印象里的“标签”可能是「代码位置标记」
goto l1 是“无条件跳转到 l1 标签标记的位置”，l1 是代码的一个具体位置。
所以 goto 就是直接跳转，执行从标签开始的代码。
但是 break l1 中的“标签”指的是“跳出哪一个循环”
   在 break l1 里，l1 其实是标记了循环代码块的开始位置，是告诉编译器“我要跳出哪个循环”。
但编译器不会跳回 l1 标签处执行代码（那样是 goto l1）。
而是把跳转目标定位在该循环对应代码块的结束位置（即循环体的末尾，跳出循环后继续执行的位置）。
换句话说，break l1 这个标签“名称”对应的是循环块，但跳转的目标地址是循环块后面的代码，不是标签声明的地方。

```go
package main

import "fmt"
import "time"

func main() {
	exitC1 := make(chan struct{})

	go func() {
	l1:
		for {
			select {
			case <-time.After(time.Second):
				fmt.Println("又等了一秒")
			case <-exitC1:
				fmt.Println("你丫的终于让我退出了")
				break l1
			}
		}
	}()

	time.Sleep(3 * time.Second)
	exitC1 <- struct{}{}
	time.Sleep(5 * time.Second)

}

```

26 设计模式-函数适配器
把一个普通函数包装成一个接口实现的适配器。
下面的目录是要一个 实现AddService接口的
但是用户写的是一个函数userAdd
于是适配器：
函数userAdd->强制转成一个函数MyAdd->MyAdd实现AddService接口

```go
package main

import (
	"fmt"
)

type AddService interface {
	Add(a int, b int) int
}

type MyAdd func(int, int) int

func (p MyAdd) Add(a int, b int) int {
	return p(a, b)
}

func testFun(aService AddService) {
	fmt.Printf("试试%v,%v=%v\n ", 3, 2, aService.Add(3, 2))
}

func userAdd(a int, b int) int {
	return a + b + 1
}

func main() {
	fmt.Printf("我这里是client233 \n")
	testFun(MyAdd(userAdd))

}
```
27  接收者的绑定
变量.方法名 会绑定接收者，得到简化的函数签名，调用时不需要传接收者。
(*类型).方法名 是从类型取方法，得到的是函数类型，需要传入接收者参数
(类型).方法名 是从类型取方法，得到的是函数类型，需要传入接收者参数

```go
package main

import "fmt"

func main() {
	var a = &Stu{
		Name: "heelo",
	}
	//TODO 特别是下面的方法绑定的过程
	f1 := a.Say
	f1("hello")
	f2 := (*Stu).Say
	f2(a, "hwhww")
	f3 := Stu.To2
	f3(*a, "hello")

}

type Stu struct {
	Name string
}

func (s Stu) To2(str string) {
	fmt.Println("2调用试试 ", str)
}

func (s *Stu) Say(str string) {
	fmt.Println("1调用试试 ", str)
}

type SayHello interface {
	Say(str string)

	To2(str string)
}

```
30

Go 自动取地址的规则

[//]: # (当切片元素是指针类型，而你直接写了一个结构体字面量时，Go 会隐式地把它转换成该结构体的指针。)
```go

type Stu struct {
Name string
}

var a = []*Stu{ {"one"}, {"two"} } 
这种初始化语法 是等价的：
Go 语言中复合字面量（composite literal）的写法，尤其是指针类型的切片初始化
struct 可不可以不写字段名直接用 {} 初始化
当你用结构体字面量初始化时，如果你按字段定义的顺序传值，

var a = []*Stu{ &Stu{"one"}, &Stu{"two"} }
```
当切片元素是指针类型，而你直接写了一个结构体字面量时，Go 会隐式地把它转换成该结构体的指针。
var a = []*Stu{ &Stu{"one"}, &Stu{"two"} }


31
这就涉及Go语言的接口实现判定规则，方法集合（method set）定义如下：

类型 T 的方法集合包含所有接收者为 T 的方法（值接收者方法）
**类型 T 的方法集合包含所有接收者为 T 和 T 的方法（值接收者和指针接收者方法）

***那为什么我之前说“值类型变量不能调用指针接收者方法”会编译错误呢？***
那是因为，这种自动取地址调用只能发生在调用方法表达式时。
但你如果把它放在接口赋值判断里，方法集合的规则不同：
值类型的“方法集合”：只包含值接收者的方法，不包含指针接收者方法
指针类型的“方法集合”：包含值接收者和指针接收者方法

***为什么值类型方法集合不包含指针接收者方法？****
原因在于：
值类型 T 的方法集合只包含接收者是 T 的方法，不包含指针接收者 *T 方法。
这是因为值类型的方法调用不会自动“升格”成指针调用，接口判断时只能静态看类型声明的方法。
****如果把指针接收者方法放进值类型的方法集合，那么在赋接口值时，接口持有的只是值的副本，调用指针方法修改副本的数据可能引起歧义和安全问题****

***接口赋值时不自动取地址，为什么？***
接口变量存储的类型信息严格对应你赋进去的变量的类型。
如果你赋一个值类型变量给接口，接口只知道它是一个值类型，方法集合只包含值接收者方法。
接口不会帮你“自动取地址”变成指针类型，因为这样会引入数据副本和生命周期管理上的复杂性。
如果接口允许值类型隐式“变指针”，会让接口调用的行为变得不确定（比如调用指针方法时是否修改了原始数据？调用的数据是副本还是原始？

Go 语言中方法集合（Method Set）的定义
方法集合指的是某个类型拥有的全部方法的集合。这个集合决定了：
该类型能调用哪些方法
该类型是否实现了某个接口（接口方法是否都包含在方法集合内）
官方定义（Go语言规范）
给定类型 T，T 的方法集合包含所有接收者类型是 T 的方法。
给定指针类型 *T，*T 的方法集合包含所有接收者类型是 T 或 *T 的方法。


****反问：如果接口判断时允许值类型包含指针方法，会有什么问题？
假设允许了：
接口方法集含有指针接收者方法，且值类型 T 也认为实现了接口（因为自动“包含”了指针接收者方法），那么：
接口内部保存的是值的副本（复制的 T）
接口调用指针接收者方法时，相当于调用的是复制品的地址，即 &副本，修改的是接口里存的那个副本
对外部原始变量无效，会产生潜在的“假修改”行为
这与用户对接口语义的直觉相悖：一般期待接口调用会影响它包装的对***

值调用的话不会产生误解，因为还是当前
```go
type Stu struct {
    Name string
}

func (s *Stu) SetName(newName string) {
    s.Name = newName
}

func main() {
    a := Stu{"Tom"}
    a.SetName("Alice")
    fmt.Println(a.Name) // 输出 "Alice"
}

```
所以不会让人误解，因为：
调用点明确：就是在 a 上调用
修改发生在同一个变量，而不是副本
与接口赋值不同，接口赋值是把值拷贝进去，修改副本会让人迷惑

***这里“*T 实现了 TestI”的意思***，是 接口语义上的“实现”，不是指指针本身会变成某个类型，只是说类型 *T 的方法集合满足接口要求
类型实现接口的定义：如果该类型的方法集合包含接口要求的所有方法，就说这个类型实现了接口。
注意：实现接口是编译器判断的静态关系，与内存布局、指针类型本身无关
“指针类型 *T 实现接口 TestI”的意思是，类型 *T 的变量可以赋给 TestI 类型的接口变量，并且可以调用接口里定义的所有方法。


***(*TestI)(nil) 在 Go 里确实是“指向接口类型的指针的 nil 值”，也就是：***
TestI 是一个接口类型
*TestI 是“指向 TestI 接口的指针类型”
(*TestI)(nil) 是一个 nil 指针，类型是 *TestI
那为啥反射里总喜欢写 reflect.TypeOf((*TestI)(nil)).Elem()？
因为 Go 的 reflect 里：
***如果你想拿到一个类型对象（reflect.Type），你必须先有一个值（哪怕是 nil）***
reflect.TypeOf((*TestI)(nil)) 得到的类型是 “指向接口的指针” (*TestI)
调 .Elem() 就能拿到它指向的类型，也就是 TestI 这个接口类型本身。
这样就能枚举这个接口类型的声明方法集了，而不需要一个实现。

32 ***嵌套的问题***
interface 本身是方法集的集合，并不存储数据。
struct 嵌入 interface 并不是把 interface 的方法直接“拷贝”进 struct，而是让 struct 拥有一个 匿名字段，这个字段的类型是 interface。
Go 的 struct 里只有字段的内存空间，不会自动生成 interface 方法的实现


33. 
****接口嵌入接口的方法签名提升的机制，这其实完全是 编译期的类型检查逻辑，不是运行时行为***
编译器在类型系统里把方法集合并，并没有生成新的字段或复制实现
ReadWriter 嵌入了 Reader 和 Writer。

Go 编译器在解析 ReadWriter 时会做如下操作：
遍历 ReadWriter 声明的方法列表（这里没有自己的方法）。
遍历嵌入的接口（Reader 和 Writer）的方法集。
把这些方法签名 加入到 ReadWriter 的方法集。
如果有重复方法签名，则报错（接口里不允许同名冲突方法）

34
Go 的 defined type（非接口）：
重点是 类型安全
不自动继承方法，必须显式增加
防止语义混淆
方法继承的 Go 方式：
通过 组合（embedding） 而不是类型继承
更灵活，也更显式

****设计哲学：****
    “明确优于隐式”
    “组合优于继承”
    避免类型语义混淆


35 切片不会自动转换的问题
Go 里一个比较经典的“切片不能直接转换为 []interface{}”的坑。
原因主要是 类型系统的设计 —— []T 和 []interface{} 不是同一个类型，而且 Go 不会自动帮你做元素逐个装箱（boxing）。

```go

func test(args ...interface{}) {
	for _, v := range args {
		fmt.Println("打印一下嘛", v)
	}
}

func main() {
	test(1, 3, 4)

	var a = []int{1, 2, 3, 4, 5}
	test(a...) //todo 这里出问题
}

```

1. 问题原因

在 Go 里：

[]int 里的每个元素都是 int（值类型）。

[]interface{} 里的每个元素是 interface{}（引用一个任意类型的值）。

虽然 单个元素 int 可以隐式转换成 interface{}（装箱），但 切片整体 不会隐式转换成 []interface{}。

这是因为在内存布局上：

[]int 是：[ptr-to-int-array, len, cap]

[]interface{} 是：[ptr-to-interface-array, len, cap]，而每个 interface{} 结构本身有 类型信息 + 数据指针 两个字段。

所以如果编译器直接把 []int 当成 []interface{} 用，那内存解释就会乱掉，产生 类型安全问题。
***Go 设计者的选择是：不帮你自动转换，必须你自己逐个元素转换****

. 调用时的 ...

在 函数调用 里，slice... 表示 把切片的元素逐个展开传入可变参数。


37 关于interface打印的问题
```go
func main() {

	var a any
	var b any

	var m int = 17
	var n int = 18

	a = m
	b = n
	println(a)
	println(b)
	println(a == b)

	b = m
	println(a)
	println(b)
	println(a == b)

	b = int64(17)
	println(a)
	println(b)
	println(a == b)

}
```



